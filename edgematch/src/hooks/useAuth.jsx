/**
 * useAuth.jsx — re-exports from the canonical AuthContext.
 * All components import from here; the implementation lives in context/AuthContext.jsx.
 */
export { AuthProvider, useAuth } from '../context/AuthContext';
